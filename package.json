{
  "name": "playwright-xray-advanced-reporter",
  "version": "1.0.0",
  "description": "Advanced Playwright Reporter for Xray Cloud - Full API integration with Test Plans, Environments, and Attachments",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "ts-node tests/e2e-mock.test.ts",
    "prepublishOnly": "npm run build && npm test"
  },
  "keywords": [
    "playwright",
    "xray",
    "jira",
    "test-management",
    "reporter",
    "automation"
  ],
  "author": "JMer Consulting",
  "license": "MIT",
  "peerDependencies": {
    "@playwright/test": ">=1.20.0"
  },
  "dependencies": {
    "form-data": "^4.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist/**/*"
  ]
}
